[workspace]
members = ["src/mapserver", "src/JWKS-Server", "src/opencvvideo", "src/energy-monitor-tauri/src-tauri"]
default-members = ["src/mapserver"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
