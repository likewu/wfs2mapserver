[workspace]
members = ["src/mapserver", "src/JWKS-Server", "src/opencvvideo"]
default-members = ["src/mapserver"]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
